# **Common Functions**

**Traversal**
```js
    const arr = [1, 2, 3, 4];

    // For loop
    for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
    }

    // For...of
    for (const num of arr) {
    console.log(num);
    }
```

**Insertion / Deletion**